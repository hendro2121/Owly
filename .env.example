# Owly Environment Variables
# Copy to .env and fill in your values

# PostgreSQL connection (Railway sets this automatically)
DATABASE_URL=

# Anthropic API key (optional - for LLM parsing fallback)
ANTHROPIC_API_KEY=
